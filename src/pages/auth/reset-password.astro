---
import Layout from "../../layouts/Layout.astro";
import { ResetPasswordForm } from "../../components/auth/ResetPasswordForm";
import { Toaster } from "../../components/ui/sonner";

// Mark this page as requiring server-side rendering
export const prerender = false;

// In the future, redirect already logged in users to the generator page
// if (Astro.locals.user) {
//   return Astro.redirect("/generate");
// }
---

<Layout title="Resetowanie hasła - 10x Cards">
  <div class="max-w-md mx-auto p-6 bg-card border rounded-lg shadow-sm">
    <h1 class="text-2xl font-bold mb-6 text-center">Resetowanie hasła</h1>
    <ResetPasswordForm client:load />
  </div>
  <Toaster client:load />
</Layout> 
---
import Layout from "../../layouts/Layout.astro";
import { NewPasswordForm } from "../../components/auth/NewPasswordForm";
import { Toaster } from "../../components/ui/sonner";

// Mark this page as requiring server-side rendering
export const prerender = false;

// In the future, we'll need to validate the token from the URL
// If token is invalid, redirect to reset-password page
// const token = Astro.url.searchParams.get('token');
// if (!token) {
//   return Astro.redirect("/auth/reset-password");
// }
---

<Layout title="Nowe hasło - 10x Cards">
  <div class="max-w-md mx-auto p-6 bg-card border rounded-lg shadow-sm">
    <h1 class="text-2xl font-bold mb-6 text-center">Nowe hasło</h1>
    <NewPasswordForm client:load />
  </div>
  <Toaster client:load />
</Layout>
